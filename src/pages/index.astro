---
import Base from '../layouts/Base.astro'; import Cta from '../components/Cta.astro'; import home from '../content/pages/home.json'; import settings from '../content/settings.json'; import { withBase } from '../paths.js';
import specialties from '../data/specialties.json';
---
<Base title="Mandarin-Speaking Therapist NYC | Sharon Miao, LCSW" description="English and Mandarin therapy in NYC with Sharon Miao, LCSW. Support for trauma, anxiety, grief and life changes. Online in NY, NJ & CA. Free 15-minute consult." counterpart="/zh/"><section class="hero"><div class="hero-copy"><p class="kicker">{home.eyebrow}</p><h1>{home.title}</h1><p>{home.intro}</p><a class="button" href={withBase(settings.schedulingUrl)}>Book a Free 15-Minute Consultation</a><p class="booking-short">Booking is for contact details only. Please do not include sensitive information.</p><p class="availability">{home.availability}</p></div></section><section class="intro-story"><div><p class="kicker">Meet Sharon Miao, LCSW</p><h2>{home.welcomeTitle}</h2><p>{home.welcomeBody}</p><p>{home.welcomeBodyTwo}</p><p><a href={withBase("/about/")}>Meet Sharon: background & approach →</a></p></div><img src={withBase('/images/portrait-960.webp')} srcset={`${withBase('/images/portrait-480.webp')} 480w, ${withBase('/images/portrait-960.webp')} 960w`} sizes="(max-width: 800px) 85vw, 440px" width="960" height="960" loading="lazy" decoding="async" alt="Sharon Miao, LCSW, psychotherapist seated on a bench by the coast"></section><section class="soft focus-section"><div class="section-head"><p class="kicker">Areas of focus</p><h2>What we can work through together</h2></div><div class="focus-grid">{home.areas.map((x,i)=><article><h3><a href={withBase(`/${specialties[i].slug}/`)}>{x.title}</a></h3><p><em>{x.body}</em></p></article>)}</div></section><section class="testimonials"><p class="kicker">A space for you</p><div class="quote-grid">{home.testimonials.map(x=><blockquote><span aria-hidden="true">″</span><p><em>{x.quote}</em></p><cite>— {x.attribution}</cite></blockquote>)}</div></section><Cta/></Base>
<script>
  if (!window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
    document.body.classList.add('home-motion');
    const sections = document.querySelectorAll('main > section');
    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add('is-visible');
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.12, rootMargin: '0px 0px -8% 0px' });
    sections.forEach((section, index) => {
      if (index === 0) section.classList.add('is-visible');
      observer.observe(section);
    });
  }
</script>
